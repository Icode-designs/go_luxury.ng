// This hook has been retired and replaced by the server-side
// lib/products/getHomeProducts.ts, which computes real best-rated/
// best-selling ranking (this file always returned averageRating: null and
// fell back to newest-first ordering for every mode). Kept as an inert
// stub because this filesystem doesn't allow deleting the file outright.
export {};
