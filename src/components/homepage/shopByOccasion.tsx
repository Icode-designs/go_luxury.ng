// src/components/homepage/shopByOccasion.tsx
//
// REMOVED from the homepage (see src/app/(main)/page.tsx) — every link here
// pointed at a ?occasion= filter with no backing column in the product
// schema, so the whole section was dead. This file could not be deleted
// (filesystem restriction on this mounted folder blocks unlink), so it's
// left as an inert, unused stub instead. Nothing imports it.
export {};
