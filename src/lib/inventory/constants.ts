// src/lib/inventory/constants.ts
// Shared inventory thresholds — single source of truth so the dashboard's
// "low stock" panel and the Inventory admin page agree on what "low" means.
export const LOW_STOCK_THRESHOLD = 3;
